using Microsoft.CodeAnalysis.CSharp.Syntax;

namespace GigSonarBlazor.Data;

public class DataService_pseudo_code
{
    /*
    public Artist FetchArtist(string name)
    {
        var a = Dblookup(name);
        if (a == null)
            a = APILookup(name);
            Db.artists.add(a);

        return a;


    }

    private Artist APILookup(string name)
    {

    }
    */
}